[project]
name = "runway-worker"
version = "0.0.0"
description = "Temporal workflows and activities for Runway deployments."
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/runway_worker"]
