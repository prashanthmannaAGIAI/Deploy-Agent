[project]
name = "runway-agent"
version = "0.0.0"
description = "Runway AI agent: repo scanner, generators, failure diagnosis, chat."
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/runway_agent"]
