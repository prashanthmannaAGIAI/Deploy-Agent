[project]
name = "runway-spec"
version = "0.0.0"
description = "Runway deploy spec (runway.yaml): Pydantic models and JSON Schema."
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/runway_spec"]
